# Send It — next quality slices

Prioritize depth, precision and readability over feature count.

1. **Official static Berlin import pipeline**
   - Detailnetz + Adressen WFS at build time
   - Ringbahn-oriented clipping/simplification
   - reproducible generated static graph
2. **Real browser playtest / telemetry pass**
   - task-rail comprehension
   - hover usefulness
   - city-expansion pacing
   - rider-energy readability
3. **Rider personality balance**
   - measure competing-call outcomes across deterministic scenarios
   - prevent one personality from dominating
4. **Event counterplay depth**
   - closures, weather cells, venue-release demand, transit-outage demand
   - every event forecasted and counterable
5. **Post-shift timeline**
   - show the sequence around collapse: bandwidth, jobs, rider breaks, events, interventions
6. **Progression tuning**
   - ensure Center → Inner City → Ring expansion feels earned and legible
   - do not add meta-progression until the single-run loop is consistently strong
7. **No second city yet**
   - Berlin remains the quality benchmark until its geography and dispatch loop are excellent.
