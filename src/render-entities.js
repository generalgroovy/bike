import { DELIVERY_TYPES,RADIO_CHANNELS } from './game-data.js';

export function drawEntities(r){drawJobPreview(r);drawAttention(r);drawRoutes(r);drawRiderTrails(r);drawDeliveries(r);drawCouriers(r);}
function reducedMotion(){return typeof window!=='undefined'&&typeof window.matchMedia==='function'&&window.matchMedia('(prefers-reduced-motion: reduce)').matches;}
function drawPath(r,path,color,width=2,dash=[]){if(!path?.length)return;const c=r.ctx,g=r.game,first=g.nodeById(path[0]);if(!first)return;c.beginPath();c.moveTo(first.x,first.y);for(let i=1;i<path.length;i++){const n=g.nodeById(path[i]);if(n)c.lineTo(n.x,n.y);}c.strokeStyle=color;c.lineWidth=r.px(width);c.setLineDash(dash.map(v=>r.px(v)));c.stroke();c.setLineDash([]);}
function focusedDelivery(g){return g.deliveryById(g.selectedDeliveryId)??g.deliveryById(g.hoveredDeliveryId);}
function drawJobPreview(r){const g=r.game,d=focusedDelivery(g);if(!d||!g.activeDeliveries().includes(d))return;const selected=g.selectedDeliveryId===d.id,c=r.ctx,p=g.nodeById(d.pickupId),q=g.nodeById(d.dropoffId);if(!p||!q)return;if(d.plannedPath)drawPath(r,d.plannedPath,selected?'#4f5b61':'#7d8588',selected?2.2:1.5,[5,5]);c.beginPath();c.moveTo(p.x,p.y);c.lineTo(q.x,q.y);c.strokeStyle='#555c60';c.lineWidth=r.px(.8);c.setLineDash([r.px(2),r.px(7)]);c.globalAlpha=selected?.45:.2;c.stroke();c.setLineDash([]);c.globalAlpha=1;if(selected){drawAddressTag(r,p.x,p.y,d.pickupAddress,'PICKUP',-1);drawAddressTag(r,q.x,q.y,d.dropoffAddress,'DROP',1);}}
function drawAddressTag(r,x,y,address,kicker,side){const c=r.ctx;if(r.zoom<.82)return;const pad=r.px(5),w=Math.max(r.px(94),c.measureText(address).width+pad*2),h=r.px(24),ox=side<0?-w-r.px(12):r.px(12),oy=-h/2;c.fillStyle='#fffdf8';c.strokeStyle='#77736b';c.lineWidth=r.px(1);c.beginPath();c.roundRect(x+ox,y+oy,w,h,r.px(4));c.fill();c.stroke();c.textAlign='left';c.textBaseline='top';c.font=`800 ${r.px(6.3)}px system-ui`;c.fillStyle='#8b857a';c.fillText(kicker,x+ox+pad,y+oy+r.px(3));c.font=`750 ${r.px(8.4)}px system-ui`;c.fillStyle='#292d2e';c.fillText(address,x+ox+pad,y+oy+r.px(11));}
function drawAttention(r){const c=r.ctx,g=r.game;for(const rider of g.couriers){if(rider.phase!=='idle'||!rider.radioOn)continue;const predicted=g.predictCall(rider);if(!predicted)continue;const pickup=g.nodeById(predicted.delivery.pickupId),progress=rider.deliberation?g.deliberationProgress(rider):0;c.beginPath();c.moveTo(rider.x,rider.y);c.lineTo(pickup.x,pickup.y);c.strokeStyle=rider.color;c.lineWidth=r.px(rider.deliberation?2:1.1);c.globalAlpha=rider.deliberation?.2+.34*progress:.09;c.setLineDash([r.px(3),r.px(7)]);c.stroke();c.setLineDash([]);c.globalAlpha=1;}}
function drawRoutes(r){const c=r.ctx,g=r.game;for(const rider of g.couriers){if(rider.path.length<2)continue;const focused=g.selectedCourierId===rider.id||g.hoveredCourierId===rider.id;c.beginPath();c.moveTo(rider.x,rider.y);for(let i=rider.pathIndex;i<rider.path.length;i++){const n=g.nodeById(rider.path[i]);if(n)c.lineTo(n.x,n.y);}c.strokeStyle='#fffdf8';c.lineWidth=r.px(focused?5.8:4.8);c.globalAlpha=.88;c.stroke();c.strokeStyle=rider.color;c.lineWidth=r.px(focused?3.4:2.7);c.globalAlpha=focused?.95:.74;c.setLineDash([r.px(6),r.px(4)]);c.stroke();c.setLineDash([]);c.globalAlpha=1;}}
function drawRiderTrails(r){if(reducedMotion()||!r.riderTrails)return;const c=r.ctx,t=performance.now()/1000;for(const rider of r.game.couriers){const trail=r.riderTrails.get(rider.id);if(!trail||trail.length<2||rider.phase==='break')continue;for(let i=1;i<trail.length;i++){const a=trail[i-1],b=trail[i],age=t-b.t;if(age>1.2)continue;const dt=Math.max(.02,b.t-a.t),speed=Math.hypot(b.x-a.x,b.y-a.y)/dt,relative=Math.max(0,Math.min(1.65,speed/Math.max(1,rider.baseSpeed))),life=Math.max(0,1-age/1.2),hot=Math.max(0,(relative-.72)/.75);c.beginPath();c.moveTo(a.x,a.y);c.lineTo(b.x,b.y);c.lineCap='round';c.strokeStyle='#e66a3f';c.lineWidth=r.px(1.4+hot*4.6);c.globalAlpha=.03+life*hot*.22;c.stroke();c.beginPath();c.moveTo(a.x,a.y);c.lineTo(b.x,b.y);c.strokeStyle=rider.color;c.lineWidth=r.px(.7+relative*1.65);c.globalAlpha=life*(.12+.38*Math.min(1,relative));c.stroke();}c.globalAlpha=1;}}
function drawDropMarker(r,d,focused){const c=r.ctx,node=r.game.nodeById(d.dropoffId);if(!node)return;c.save();c.translate(node.x,node.y);const s=r.px(focused?7:5.5);c.beginPath();c.moveTo(0,-s);c.lineTo(s,0);c.lineTo(0,s);c.lineTo(-s,0);c.closePath();c.fillStyle='#fffdf8';c.fill();c.strokeStyle='#555b5c';c.lineWidth=r.px(focused?1.5:1.1);c.stroke();c.restore();}
function drawDeliveryRipple(r,d,node,type,urgency,reduced,time){if(d.status!=='waiting')return;const c=r.ctx,seed=Number(d.id.replace(/\D/g,''))||0,period=urgency<.28?.72:d.called?1.75:3.25,phase=reduced?.22:((time+seed*.173)%period)/period,alpha=reduced?.12:(1-phase)*(urgency<.28?.28:.13),radius=r.px(13+phase*(urgency<.28?28:20));c.save();c.translate(node.x,node.y);c.beginPath();c.arc(0,0,radius,0,Math.PI*2);c.strokeStyle=urgency<.28?'#ce4556':d.called?(RADIO_CHANNELS[d.channel]?.color??type.color):type.color;c.lineWidth=r.px(urgency<.28?1.7:1.1);c.globalAlpha=alpha;c.stroke();if(!reduced&&urgency<.2){c.beginPath();c.arc(0,0,Math.max(r.px(10),radius-r.px(8)),0,Math.PI*2);c.globalAlpha=alpha*.58;c.stroke();}c.restore();}
function drawDeliveries(r){const c=r.ctx,g=r.game,reduced=reducedMotion(),time=reduced?0:performance.now()/1000;for(const d of g.activeDeliveries()){const node=g.nodeById(d.pickedUp?d.dropoffId:d.pickupId),type=DELIVERY_TYPES[d.type],urgency=g.urgency(d),selected=g.selectedDeliveryId===d.id,hovered=g.hoveredDeliveryId===d.id,focused=selected||hovered,called=d.called,claimed=d.status==='claimed',channel=RADIO_CHANNELS[d.channel],radius=r.px(focused?14:10.7);drawDeliveryRipple(r,d,node,type,urgency,reduced,time);if((focused||called||claimed)&&!d.pickedUp)drawDropMarker(r,d,focused);c.save();c.translate(node.x,node.y);c.globalAlpha=claimed?.8:1;c.beginPath();c.arc(0,0,radius+r.px(3),0,Math.PI*2);c.fillStyle='#fffdf8';c.fill();c.strokeStyle=called?(channel?.color??type.color):'#686a66';c.lineWidth=r.px(focused?3.1:called?2.45:1.3);c.stroke();c.beginPath();c.arc(0,0,radius+r.px(6),-Math.PI/2,-Math.PI/2+Math.PI*2*urgency);c.strokeStyle=urgency<.28?'#ce4556':'#6f716d';c.lineWidth=r.px(1.55);c.stroke();c.fillStyle=type.color;c.font=`900 ${r.px(11.5)}px system-ui`;c.textAlign='center';c.textBaseline='middle';c.fillText(d.pickedUp?'◎':type.glyph,0,0);if(d.specialGlyph){c.beginPath();c.arc(-radius*.82,-radius*.82,r.px(5.2),0,Math.PI*2);c.fillStyle='#303638';c.fill();c.fillStyle='#fff';c.font=`900 ${r.px(5.8)}px system-ui`;c.fillText(d.specialGlyph,-radius*.82,-radius*.82);}if(called&&!claimed){const pulses=d.channel==='priority'?3:2;for(let i=0;i<pulses;i++){c.beginPath();const rr=radius+r.px(9+i*4+(reduced?0:Math.sin(time*4+i)));c.arc(0,0,rr,-.58,.58);c.strokeStyle=channel.color;c.lineWidth=r.px(1.2);c.globalAlpha=.36-i*.08;c.stroke();c.beginPath();c.arc(0,0,rr,Math.PI-.58,Math.PI+.58);c.stroke();}c.globalAlpha=1;c.beginPath();c.arc(radius*.75,-radius*.75,r.px(5),0,Math.PI*2);c.fillStyle=channel.color;c.fill();c.fillStyle='#fff';c.font=`900 ${r.px(5.5)}px system-ui`;c.fillText(d.channel==='priority'?'!':d.channel==='local'?'L':'O',radius*.75,-radius*.75);}if(r.zoom>=1.18||focused){c.font=`800 ${r.px(7.4)}px ui-monospace,monospace`;c.fillStyle='#343838';c.fillText(d.id.toUpperCase(),0,radius+r.px(10));}c.restore();}}
function drawMotionCue(r,rider,radius,reduced){if(reduced||!(rider.phase==='pickup'||rider.phase==='dropoff')||!Number.isFinite(rider.heading))return;const c=r.ctx,t=performance.now()/1000,pulse=.72+.18*Math.sin(t*8+rider.id.length);c.save();c.rotate(rider.heading);c.strokeStyle=rider.color;c.lineWidth=r.px(1.25);c.globalAlpha=.18*pulse;for(let i=-1;i<=1;i++){c.beginPath();c.moveTo(r.px(i*3.2),radius+r.px(4));c.lineTo(r.px(i*3.2),radius+r.px(8+Math.abs(i)*2));c.stroke();}c.restore();}
function drawMilestoneCue(r,rider,radius,reduced){if(!rider.lastMilestone||!Number.isFinite(rider.lastMilestoneAt))return;const age=r.game.elapsed-rider.lastMilestoneAt;if(age<0||age>1.15)return;const c=r.ctx,progress=Math.max(0,Math.min(1,age/1.15)),alpha=reduced?.34:.42*(1-progress),ring=radius+r.px(reduced?7:5+progress*15);c.save();c.strokeStyle=rider.color;c.lineWidth=r.px(reduced?1.4:1.8-progress*.8);c.globalAlpha=alpha;c.beginPath();c.arc(0,0,ring,0,Math.PI*2);c.stroke();if(!reduced){c.beginPath();c.arc(0,0,radius+r.px(4+progress*8),0,Math.PI*2);c.globalAlpha=alpha*.55;c.stroke();}c.globalAlpha=Math.max(.22,alpha);c.fillStyle=rider.color;c.font=`900 ${r.px(6.2)}px system-ui`;c.textAlign='center';c.textBaseline='middle';c.fillText(rider.lastMilestone==='pickup'?'P':'✓',radius+r.px(8),-radius-r.px(5));c.restore();}
function drawCouriers(r){const c=r.ctx,g=r.game,reduced=reducedMotion();for(const rider of g.couriers){const selected=g.selectedCourierId===rider.id,hovered=g.hoveredCourierId===rider.id,focused=selected||hovered,onBreak=rider.phase==='break'||!rider.radioOn,thinking=g.deliberationProgress(rider),task=g.courierTaskProgress(rider),radius=r.px(focused?12.8:10.4);c.save();c.translate(rider.x,rider.y);drawMilestoneCue(r,rider,radius,reduced);drawMotionCue(r,rider,radius,reduced);if(rider.deliberation&&!onBreak){c.beginPath();c.arc(0,0,radius+r.px(6),-Math.PI/2,-Math.PI/2+Math.PI*2*thinking);c.strokeStyle='#313535';c.lineWidth=r.px(1.8);c.stroke();}else if(rider.deliveryId&&!onBreak){c.beginPath();c.arc(0,0,radius+r.px(6),-Math.PI/2,-Math.PI/2+Math.PI*2*task);c.strokeStyle=rider.color;c.globalAlpha=.85;c.lineWidth=r.px(2);c.stroke();c.globalAlpha=1;}c.beginPath();c.arc(0,0,radius+r.px(2),0,Math.PI*2);c.fillStyle='#fffdf8';c.fill();c.strokeStyle=onBreak?'#999b97':rider.color;c.lineWidth=r.px(focused?3:2.1);c.stroke();c.save();if(!onBreak&&Number.isFinite(rider.heading))c.rotate(rider.heading);c.fillStyle=onBreak?'#a3a5a1':rider.color;c.beginPath();c.moveTo(0,-radius*.72);c.lineTo(radius*.72,radius*.62);c.lineTo(-radius*.72,radius*.62);c.closePath();c.fill();c.restore();c.fillStyle='#fff';c.font=`900 ${r.px(7)}px system-ui`;c.textAlign='center';c.textBaseline='middle';c.fillText(onBreak?'Ⅱ':rider.experience.level,0,r.px(.4));if(r.zoom>=1.16||focused){c.font=`800 ${r.px(8.2)}px system-ui`;c.strokeStyle='#f3f0e8';c.lineWidth=r.px(2.6);c.strokeText(rider.name,0,radius+r.px(11));c.fillStyle=onBreak?'#777a76':'#303535';c.fillText(rider.name,0,radius+r.px(11));}c.restore();}}
